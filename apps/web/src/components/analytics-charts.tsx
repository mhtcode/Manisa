type Point = { name: string; value: number };
function Chart({ data, tone }: { data: Point[]; tone: "teal" | "sky" }) {
  if (!data.length) return <div className="empty">Complete appointments to see this chart.</div>;
  const maximum = Math.max(...data.map((point) => point.value), 1);
  const barColor = tone === "teal" ? "bg-teal-300" : "bg-sky-300";
  return <div className="space-y-3 px-1 py-1" aria-label="Business analytics chart" role="img">{data.map((point) => <div key={point.name}><div className="mb-1.5 flex items-center justify-between gap-3 text-xs"><span className="min-w-0 truncate text-slate-400" dir="auto">{point.name}</span><span className="shrink-0 font-semibold tabular-nums text-slate-200">{new Intl.NumberFormat("en-CA", { style: "currency", currency: "CAD", maximumFractionDigits: 0 }).format(point.value)}</span></div><div className="h-2 overflow-hidden rounded-full bg-white/[0.055]"><div className={`h-full rounded-full ${barColor}`} style={{ width: `${Math.max(3, (point.value / maximum) * 100)}%` }} /></div></div>)}</div>;
}
export function AnalyticsCharts({ revenue, services }: { revenue: Point[]; services: Point[] }) { return <div className="grid gap-4 xl:grid-cols-2"><section className="panel"><div className="panel-header py-3.5"><div><h2 className="text-sm font-medium">Revenue over time</h2><p className="mt-1 text-[11px] text-slate-500">Completed appointment revenue</p></div></div><div className="p-4 sm:p-5"><Chart data={revenue} tone="teal"/></div></section><section className="panel"><div className="panel-header py-3.5"><div><h2 className="text-sm font-medium">Revenue by service</h2><p className="mt-1 text-[11px] text-slate-500">Booked service-line value</p></div></div><div className="p-4 sm:p-5"><Chart data={services} tone="sky"/></div></section></div>; }
