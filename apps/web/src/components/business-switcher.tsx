import { Building2, Check } from "lucide-react";
import { switchWorkspace } from "@/server/actions/platform";
export function BusinessSwitcher({ currentId, items }: { currentId: string; items: Array<{ businessId: string; name: string }> }) { if (items.length < 2) return null; return <details className="relative"><summary aria-label="Switch business" className="icon-button cursor-pointer list-none"><Building2 size={17}/></summary><div className="absolute end-0 top-[calc(100%+.5rem)] z-50 min-w-56 overflow-hidden rounded-2xl border border-white/10 bg-[#0d1522] p-1 shadow-2xl">{items.map((item) => <form action={switchWorkspace} key={item.businessId}><input name="businessId" type="hidden" value={item.businessId}/><button className="flex w-full items-center gap-2 rounded-xl px-3 py-2.5 text-start text-sm hover:bg-blue-500/10"><span className="min-w-0 flex-1 truncate">{item.name}</span>{item.businessId === currentId ? <Check className="text-blue-300" size={15}/> : null}</button></form>)}</div></details>; }
