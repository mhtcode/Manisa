import Link from "next/link";
import { ArrowLeft, CircleHelp } from "lucide-react";
import { DismissiblePopover } from "@/components/dismissible-popover";

export function PageHeading({ title, description, actions, backHref, backLabel = "Back to Settings" }: { title: string; description?: string; actions?: React.ReactNode; backHref?: string; backLabel?: string }) { return <div className="mb-5 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div className="flex min-w-0 items-center gap-2">{backHref && <Link aria-label={backLabel} className="icon-button shrink-0 rtl:order-last" href={backHref} title={backLabel}><ArrowLeft size={18}/></Link>}<h1 className="page-title truncate">{title}</h1>{description && <DismissiblePopover ariaLabel="Help" panelClassName="fixed inset-x-3 top-[7.5rem] z-50 max-h-[calc(100dvh-9rem)] overflow-y-auto rounded-xl border border-blue-300/15 bg-[#101927] p-3 text-xs leading-5 text-slate-300 shadow-2xl sm:absolute sm:inset-x-auto sm:start-0 sm:top-10 sm:w-80" rootClassName="relative z-40 shrink-0" trigger={<CircleHelp size={17}/>} triggerClassName="flex size-8 items-center justify-center rounded-full text-slate-500 transition hover:bg-white/[0.05] hover:text-blue-300">{description}</DismissiblePopover>}</div>{actions && <div className="flex min-w-0 flex-wrap gap-2">{actions}</div>}</div>; }
